extern func printf(...);

# prog program :: c : -int = {
prog program :: io : -int = {
    var a, b, c, d, e; #, f, K;

    a := b; 
    b := c; 
    c := d; 
    d := a; 

    e := 1; 

    a := e; 

    # return -1;
    io.send(-1)
}