extern int func printf(str s, ...);

define program :: c : Channel<-int> = {

    printf("Prior to decl\n");
    
    proc inner() {
        printf("Hey friends!\n");
    }

    inner(); 

    return -1; 
}