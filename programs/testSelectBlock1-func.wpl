extern int func printf(...);

# str func selReturnOne(int a) {
define func selReturnOne (int a) : str {
    return "CASE IMPOSSIBLE (-1)"; 
}

define program :: c : Channel<-int> = {

    var i := -10; 
    while i <= 15 {
        printf("f(%d) = %s\n", i, selReturnOne(i));
        i := i + 1;
    }
    
    # return -1; 
    c.send(-1)
}
