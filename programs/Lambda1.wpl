extern int func printf(...);

int func foo(int x) {
    printf("FOO: %u\n", x);
    return x; 
}


define program :: c : Channel<-int> = {
    var ref := foo; 

    ref(10); 
    foo(11); 

    return 0;
}