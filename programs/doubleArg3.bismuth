extern func printf(...);

# proc foo(int a, str a, int b) {
func foo (int a, str a, int b) -> int {
    a := 10;
    a := "10";  

    var c := a; 

    return 0;
}

prog program :: c : -int = {
 var current := 1;        
 c.send(current)
#  return -1;
}