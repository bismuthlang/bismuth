extern int func printf(...);



define program :: c : Channel<-int> = {
    if 1 < 2 then {
        printf("Correct!\n");
    }

    if 2 < 1 then {
        printf("Not correct!\n");
    }

    # return -1; 
    c.send(-1)
}