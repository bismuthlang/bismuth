# extern int func getArgCount();
# define program :: c : Channel<-int> = {
#   int i;
#   i := getArgCount();
#   return i;
# }

extern int func getArgCount();

define program :: c : Channel<-int> = {
  int i;
  i := getArgCount();
  c.send(i)
}