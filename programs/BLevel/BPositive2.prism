# define program :: c : Channel<-int> = {
#   int i := 31;
#   int b;
#   if (i < 42) then {
#     b := 0;
#   } else {
#     b := 42;
#   }
#   return b;
# }

define program :: c : Channel<-int> = {
  int i := 31;
  int b;
  if (i < 42) then {
    b := 0;
  } else {
    b := 42;
  }
  c.send(b)
}