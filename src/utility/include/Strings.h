/**
 * @file Strings.h
 * @author Alex Friedman (ahf.dev)
 * @brief Utility functions for strings 
 * @version 0.1
 * @date 2024-02-15
 * 
 * @copyright Copyright (c) 2024
 * 
 */

/*
#include <vector>
#include <string>

namespace util {
    namespace Strings {
        using std::vector; 
        using std::string; 
        using std::size_t; 

        vector<string> split(string str, string delim) 
        {
            vector<string> ans; 

            size_t cur = 0; 
            size_t idx;// = str.find(delim); 

            while((idx = str.find(delim, cur)) != string::npos)
            {
                ans.push_back(
                    str.substr(cur, idx - cur)
                ); 

                cur = idx; 
            }
            
            ans.push_back(
                str.substr(cur, idx - cur)
            ); 

            return ans; 
        }
    }
}
*/